name: logistics-platform

services:

  # 🐘 Единая PostgreSQL для всей логистической платформы
  postgres:
    image: postgres:16-alpine
    container_name: logistics-postgres_db
    environment:
      - POSTGRES_DB=logistics_platform_db
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - TZ=Europe/Moscow  # 👈 ПОСТГРЕС в часовой пояс МСК (GMT+3)
    ports:
      - "5433:5432"  # 👈 1. Изменили ВНЕШНИЙ порт на 5433 для предотвращения конфликтов
    volumes:
      - postgres_data:/var/lib/postgresql/data # Так контейнер НЕ использует анонимный volume, который удаляется при docker-compose down -v
      - ./postgres:/docker-entrypoint-initdb.d
    healthcheck: # Проверка готовности PostgreSQL принимать запросы
      test: [ "CMD-SHELL", "pg_isready -U postgres -d logistics_platform_db" ]
      interval: 5s
      timeout: 5s
      retries: 5

  # 🐘 Веб-интерфейс для PostgreSQL и MongoDB (без авто-подключений)
  dbgate:
    image: dbgate/dbgate:latest
    container_name: logistics-dbgate
    environment:
      # Таймзона
      - TZ=Europe/Moscow
    ports:
      - "5051:3000"
    volumes:
      - dbgate_data:/root/.dbgate
    depends_on:
      postgres:
        condition: service_healthy
      mongodb:
        condition: service_healthy

  # 🍃 MongoDB для документов и картинок Cargo Service
  mongodb:
    image: mongo:7
    container_name: logistics-mongo_db
    environment:
#      - MONGO_INITDB_ROOT_USERNAME=root  # 👈 ЗАКОММЕНТИРОВАЛ для разработки, потом включу (без аутентификации для разработки)
#      - MONGO_INITDB_ROOT_PASSWORD=root
      - MONGO_INITDB_DATABASE=logistics_cargo_mongo_db  # 👈 1. Поменяли имя дефолтной БД
      - TZ=Europe/Moscow        # 👈 НАУЧИТ МОНГУ ВРЕМЕНИ МСК (GMT+3)
    ports:
      - "27018:27017"  # 👈 2. Поменяли ВНЕШНИЙ порт на 27018. Внутри контейнера всегда 27017.
    volumes:
      - mongo_data:/data/db   # 👈именую volume
    healthcheck: # Проверка готовности MongoDB
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 5s
      timeout: 5s
      retries: 5

  # 🔐 Инфраструктура Keycloak
  postgres-keycloak:
    image: postgres:16-alpine
    container_name: logistics-postgres-keycloak
    environment:
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_USER=postgres
      - POSTGRES_DB=keycloak
      - TZ=Europe/Moscow        # 👈 ПОСТГРЕС в часовой пояс МСК (GMT+3)
    ports:
      - "5434:5432"
    volumes:
      - postgres_keycloak_data:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres" ]
      interval: 5s
      timeout: 5s
      retries: 5

  keycloak:
    image: quay.io/keycloak/keycloak:26.0.7
    container_name: logistics-keycloak
    depends_on:
      postgres-keycloak:
        condition: service_healthy # Keycloak честно дождется готовности базы!
    command: [ "start-dev", "--import-realm" ]
    ports:
      - "8080:8080"
    environment:
      - TZ=Europe/Moscow      # 👈 КЕЙКЛОК в часовой пояс МСК (GMT+3)
      - KC_BOOTSTRAP_ADMIN_USERNAME=admin
      - KC_BOOTSTRAP_ADMIN_PASSWORD=admin
      - KC_DB=postgres
      - KC_DB_URL_HOST=postgres-keycloak
      - KC_DB_URL_PORT=5432
      - KC_DB_URL_DATABASE=keycloak
      - KC_DB_USERNAME=postgres
      - KC_DB_PASSWORD=postgres
    volumes:
      - ./keycloak/realms:/opt/keycloak/data/import
      - ./keycloak/exports:/tmp/keycloak-export

  # 📨 Kafka Broker в режиме KRaft (без ZooKeeper)
  kafka:
    image: apache/kafka:latest  # 👈 Официальный образ от Apache
    container_name: logistics-kafka
    ports:
      - "9092:9092"   # Внутренний порт (для Docker)
      - "9094:9094"   # Внешний порт (для Spring / IDEA на ПК)
    environment:
      # ======== НАСТРОЙКИ РЕЖИМА KRaft ========
      # Уникальный ID узла в кластере
      - KAFKA_NODE_ID=1

      # Роли: controller (управление кластером) + broker (обработка сообщений)
      - KAFKA_PROCESS_ROLES=controller,broker

      # Список узлов, которые могут участвовать в выборах лидера
      # Формат: id@hostname:port (для одного узла)
      - KAFKA_CONTROLLER_QUORUM_VOTERS=1@kafka:9093

      # ======== НАСТРОЙКИ СЛУШАТЕЛЕЙ ========
      # На каких адресах и портах слушает Kafka
      # - PLAINTEXT://:9092 - для клиентских подключений
      # - CONTROLLER://:9093 - для внутреннего общения контроллеров

      # Слушаем на всех интерфейсах для PLAINTEXT (9092), EXTERNAL (9094) и CONTROLLER (9093)
      - KAFKA_LISTENERS=PLAINTEXT://:9092,EXTERNAL://:9094,CONTROLLER://:9093

      # Объявляем адреса: внутри сети Docker хост 'kafka', а снаружи — 'localhost'
      - KAFKA_ADVERTISED_LISTENERS=PLAINTEXT://kafka:9092,EXTERNAL://localhost:9094

      # Сопоставляем имена слушателей с протоколами
      - KAFKA_LISTENER_SECURITY_PROTOCOL_MAP=PLAINTEXT:PLAINTEXT,EXTERNAL:PLAINTEXT,CONTROLLER:PLAINTEXT

      # Имя слушателя для контроллера
      - KAFKA_CONTROLLER_LISTENER_NAMES=CONTROLLER

      # Имя слушателя для общения между брокерами
      - KAFKA_INTER_BROKER_LISTENER_NAME=PLAINTEXT

      # ======== НАСТРОЙКИ ХРАНЕНИЯ ========
      # Коэффициент репликации для системных топиков (для одного узла = 1)
      - KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR=1
      - KAFKA_TRANSACTION_STATE_LOG_MIN_ISR=1
      - KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR=1

      # Автоматическое создание топиков при первой отправке сообщения
      - KAFKA_AUTO_CREATE_TOPICS_ENABLE=true

      # Путь для хранения логов (должен совпадать с volume)
      - KAFKA_LOG_DIRS=/var/lib/kafka/data

      # Таймзона
      - TZ=Europe/Moscow
    volumes:
      - kafka_data:/var/lib/kafka/data
    user: "1000:1000"  # 🟢 Запускаем от пользователя с правами
    healthcheck:
#      test: ["CMD-SHELL", "exec 3<>/dev/tcp/127.0.0.1/9092 || exit 1"]
      test: [ "CMD-SHELL", "nc -z localhost 9092 || exit 1" ]
      interval: 10s
      timeout: 10s
      retries: 5

  # 📊 Веб-интерфейс для мониторинга Kafka
  kafka-ui:
    image: provectuslabs/kafka-ui:latest
    container_name: logistics-kafka-ui
    depends_on:
      kafka:
        condition: service_healthy
    ports:
      - "8090:8080"
    environment:
      - KAFKA_CLUSTERS_0_NAME=local
      - KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS=kafka:9092
      - TZ=Europe/Moscow

#  # ============================================================
#  # 🚀 МИКРОСЕРВИСЫ
#  # ============================================================
#
#  # 🚀 API Gateway
#  api-gateway:
#    image: logistics-platform-api-gateway:latest
#    container_name: logistics-api-gateway
#    ports:
#      - "8088:8080"
#    environment:
#      # Каждый микросервис должен видеть Keycloak по имени keycloak (внутри Docker сети)
#      - SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_ISSUER_URI=http://keycloak:8080/realms/logistics-platform
#      - SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_JWK_SET_URI=http://keycloak:8080/realms/logistics-platform/protocol/openid-connect/certs
#      - TZ=Europe/Moscow
#    depends_on:
#      keycloak:
#        condition: service_healthy
#      postgres:
#        condition: service_healthy
#    networks:
#      - default
#
#  # 🚀 User Service
#  user-service:
#    image: logistics-platform-user-service:latest
#    container_name: logistics-user-service
#    ports:
#      - "8081:8081"
#    environment:
#      - SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_ISSUER_URI=http://keycloak:8080/realms/logistics-platform
#      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/logistics_platform_db
#      - SPRING_DATASOURCE_USERNAME=postgres
#      - SPRING_DATASOURCE_PASSWORD=postgres
#      - SPRING_DATA_MONGODB_URI=mongodb://mongodb:27017/logistics_user_mongo_db
#      - SPRING_KAFKA_BOOTSTRAP_SERVERS=kafka:9092
#      - TZ=Europe/Moscow
#    depends_on:
#      postgres:
#        condition: service_healthy
#      mongodb:
#        condition: service_healthy
#      kafka:
#        condition: service_healthy
#      keycloak:
#        condition: service_healthy
#    networks:
#      - default
#
#  # 🚀 Delivery Service
#  delivery-service:
#    image: logistics-platform-delivery-service:latest
#    container_name: logistics-delivery-service
#    ports:
#      - "8082:8082"
#    environment:
#      - SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_ISSUER_URI=http://keycloak:8080/realms/logistics-platform
#      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/logistics_platform_db
#      - SPRING_DATASOURCE_USERNAME=postgres
#      - SPRING_DATASOURCE_PASSWORD=postgres
#      - SPRING_KAFKA_BOOTSTRAP_SERVERS=kafka:9092
#      - TZ=Europe/Moscow
#    depends_on:
#      postgres:
#        condition: service_healthy
#      kafka:
#        condition: service_healthy
#      keycloak:
#        condition: service_healthy
#    networks:
#      - default
#
#  # 🚀 Transport Service
#  transport-service:
#    image: logistics-platform-transport-service:latest
#    container_name: logistics-transport-service
#    ports:
#      - "8083:8083"
#    environment:
#      - SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_ISSUER_URI=http://keycloak:8080/realms/logistics-platform
#      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/logistics_platform_db
#      - SPRING_DATASOURCE_USERNAME=postgres
#      - SPRING_DATASOURCE_PASSWORD=postgres
#      - SPRING_KAFKA_BOOTSTRAP_SERVERS=kafka:9092
#      - TZ=Europe/Moscow
#    depends_on:
#      postgres:
#        condition: service_healthy
#      kafka:
#        condition: service_healthy
#      keycloak:
#        condition: service_healthy
#    networks:
#      - default
#
#  # 🚀 Payment Service
#  payment-service:
#    image: logistics-platform-payment-service:latest
#    container_name: logistics-payment-service
#    ports:
#      - "8084:8084"
#    environment:
#      - SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_ISSUER_URI=http://keycloak:8080/realms/logistics-platform
#      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/logistics_platform_db
#      - SPRING_DATASOURCE_USERNAME=postgres
#      - SPRING_DATASOURCE_PASSWORD=postgres
#      - SPRING_KAFKA_BOOTSTRAP_SERVERS=kafka:9092
#      - TZ=Europe/Moscow
#    depends_on:
#      postgres:
#        condition: service_healthy
#      kafka:
#        condition: service_healthy
#      keycloak:
#        condition: service_healthy
#    networks:
#      - default
#
#  # 🚀 Cargo Service
#  cargo-service:
#    image: logistics-platform-cargo-service:latest
#    container_name: logistics-cargo-service
#    ports:
#      - "8085:8085"
#    environment:
#      - SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_ISSUER_URI=http://keycloak:8080/realms/logistics-platform
#      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/logistics_platform_db
#      - SPRING_DATASOURCE_USERNAME=postgres
#      - SPRING_DATASOURCE_PASSWORD=postgres
#      - SPRING_DATA_MONGODB_URI=mongodb://mongodb:27017/logistics_cargo_mongo_db
#      - SPRING_KAFKA_BOOTSTRAP_SERVERS=kafka:9092
#      - TZ=Europe/Moscow
#    depends_on:
#      postgres:
#        condition: service_healthy
#      mongodb:
#        condition: service_healthy
#      kafka:
#        condition: service_healthy
#      keycloak:
#        condition: service_healthy
#    networks:
#      - default
#
#  # 🚀 Notification Service
#  notification-service:
#    image: logistics-platform-notification-service:latest
#    container_name: logistics-notification-service
#    ports:
#      - "8086:8086"
#    environment:
#      - SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_ISSUER_URI=http://keycloak:8080/realms/logistics-platform
#      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/logistics_platform_db
#      - SPRING_DATASOURCE_USERNAME=postgres
#      - SPRING_DATASOURCE_PASSWORD=postgres
#      - SPRING_DATA_MONGODB_URI=mongodb://mongodb:27017/logistics_cargo_mongo_db
#      - SPRING_KAFKA_BOOTSTRAP_SERVERS=kafka:9092
#      - TZ=Europe/Moscow
#    depends_on:
#      postgres:
#        condition: service_healthy
#      mongodb:
#        condition: service_healthy
#      kafka:
#        condition: service_healthy
#      keycloak:
#        condition: service_healthy
#    networks:
#      - default
#
#  # 🚀 Reporting Service
#  reporting-service:
#    image: logistics-platform-reporting-service:latest
#    container_name: logistics-reporting-service
#    ports:
#      - "8087:8087"
#    environment:
#      - SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_ISSUER_URI=http://keycloak:8080/realms/logistics-platform
#      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/logistics_platform_db
#      - SPRING_DATASOURCE_USERNAME=postgres
#      - SPRING_DATASOURCE_PASSWORD=postgres
#      - SPRING_DATA_MONGODB_URI=mongodb://mongodb:27017/logistics_cargo_mongo_db
#      - SPRING_KAFKA_BOOTSTRAP_SERVERS=kafka:9092
#      - TZ=Europe/Moscow
#    depends_on:
#      postgres:
#        condition: service_healthy
#      mongodb:
#        condition: service_healthy
#      kafka:
#        condition: service_healthy
#      keycloak:
#        condition: service_healthy
#    networks:
#      - default

# ============================================================
# 🌐 СЕТИ
# ============================================================

networks:
  default:
    name: logistics-network
    driver: bridge

# ============================================================
# 📦 VOLUMES
# ============================================================

volumes:
  postgres_data:
  mongo_data:
  postgres_keycloak_data:
  dbgate_data:
  kafka_data: